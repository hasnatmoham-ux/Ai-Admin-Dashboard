import { Bot, Sparkles, ShieldCheck, Workflow } from 'lucide-react';
import { useNavigate } from 'react-router-dom';
import { useAppStore } from '../store/useAppStore.js';
export default function Login(){const login=useAppStore(s=>s.login);const nav=useNavigate();const submit=e=>{e.preventDefault();login();nav('/')};return <div className="login-page"><section className="login-hero"><div className="brand"><div className="brand-icon"><Bot/></div><span>AdminAI</span></div><h1 className="hero-title">AI-powered internal admin operations.</h1><p className="page-subtitle" style={{fontSize:18,maxWidth:580}}>Manage users, approvals, workflows, data records, audit logs, and AI admin decisions from one modern internal dashboard.</p><div className="grid grid-3" style={{marginTop:30}}><div className="card"><Sparkles/> AI summaries</div><div className="card"><ShieldCheck/> Secure roles</div><div className="card"><Workflow/> Workflow ops</div></div></section><form className="login-card" onSubmit={submit}><h2 style={{marginTop:0}}>Sign in</h2><p className="page-subtitle">Demo credentials are prefilled for portfolio preview.</p><div className="form-control"><label>Email</label><input defaultValue="admin@company.com" /></div><div className="form-control" style={{marginTop:14}}><label>Password</label><input type="password" defaultValue="password" /></div><button className="btn btn-primary" style={{width:'100%',justifyContent:'center',marginTop:20}}>Sign in to dashboard</button><button type="button" className="btn btn-secondary" onClick={submit} style={{width:'100%',justifyContent:'center',marginTop:12}}>Use demo login</button></form></div>}
