import { AreaChart, Area, BarChart, Bar, CartesianGrid, XAxis, YAxis, Tooltip, ResponsiveContainer } from 'recharts';
import { Users, ClipboardCheck, AlertTriangle, DollarSign, Sparkles, Plus, Download } from 'lucide-react';
import PageHeader from '../components/common/PageHeader.jsx';
import Badge, { toneFor } from '../components/common/Badge.jsx';
import { kpis, chartData, approvals, alerts, logs } from '../data/mockData.js';
const icons={Users,ClipboardCheck,AlertTriangle,DollarSign};
export default function Dashboard(){return <section className="page"><PageHeader title="Operations Dashboard" subtitle="Live view of internal tools, approvals, alerts, and AI operational insights." actions={<><button className="btn btn-secondary"><Download size={17}/>Export Report</button><button className="btn btn-primary"><Plus size={17}/>Create Workflow</button></>}/><div className="grid grid-4">{kpis.map(k=>{const Icon=icons[k.icon];return <div className="card kpi" key={k.label}><div><div className="label">{k.label}</div><div className="value">{k.value}</div><Badge tone="blue">{k.change}</Badge></div><div className="kpi-icon"><Icon/></div></div>})}</div><div className="grid grid-2" style={{marginTop:18}}><div className="card"><h3>Admin actions over time</h3><ResponsiveContainer height={280}><AreaChart data={chartData}><CartesianGrid strokeDasharray="3 3"/><XAxis dataKey="name"/><YAxis/><Tooltip/><Area type="monotone" dataKey="actions" stroke="currentColor" fill="currentColor" fillOpacity={0.12}/></AreaChart></ResponsiveContainer></div><div className="card"><h3>Ticket volume by category</h3><ResponsiveContainer height={280}><BarChart data={chartData}><CartesianGrid strokeDasharray="3 3"/><XAxis dataKey="name"/><YAxis/><Tooltip/><Bar dataKey="tickets" fill="currentColor" radius={[8,8,0,0]}/></BarChart></ResponsiveContainer></div></div><div className="grid grid-3" style={{marginTop:18}}><div className="ai-panel"><h3><Sparkles size={18}/> AI insights</h3><p>Refund requests are 42% above baseline. AI recommends reviewing Acme Health and enterprise discount approvals first because both have high revenue impact.</p><button className="btn btn-primary">Run AI Analysis</button></div><div className="card"><h3>Pending approvals</h3>{approvals.slice(0,3).map(a=><p key={a.id}><b>{a.type}</b><br/><span className="page-subtitle">{a.customer} • {a.amount}</span></p>)}</div><div className="card"><h3>Recent activity</h3><div className="timeline">{logs.slice(0,3).map((l,i)=><div className="timeline-item" key={i}><span className="dot"/><div><b>{l.actor}</b> {l.action}<br/><span className="page-subtitle">{l.time}</span></div></div>)}</div></div></div><div className="card" style={{marginTop:18}}><h3>System alerts</h3>{alerts.map(a=><div key={a.title} style={{display:'flex',justifyContent:'space-between',padding:'12px 0',borderBottom:'1px solid var(--border)'}}><span>{a.title}</span><Badge tone={toneFor(a.severity)}>{a.severity}</Badge></div>)}</div></section>}
