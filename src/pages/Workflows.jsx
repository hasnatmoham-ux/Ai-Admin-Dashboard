import { Play, Pause, Activity } from 'lucide-react';
import PageHeader from '../components/common/PageHeader.jsx';
import Badge, { toneFor } from '../components/common/Badge.jsx';
import { workflows } from '../data/mockData.js';
export default function Workflows(){return <section className="page"><PageHeader title="Workflows" subtitle="Monitor automation flows, triggers, run history, and approval steps." actions={<button className="btn btn-primary">Create Workflow</button>}/><div className="grid grid-2">{workflows.map(w=><div className="card" key={w.name}><div style={{display:'flex',justifyContent:'space-between'}}><h3>{w.name}</h3><Badge tone={toneFor(w.status)}>{w.status}</Badge></div><p className="page-subtitle">Trigger: {w.trigger}</p><div className="grid grid-3"><p><b>{w.success}</b><br/><span className="page-subtitle">Success</span></p><p><b>{w.steps}</b><br/><span className="page-subtitle">Steps</span></p><p><b>{w.lastRun}</b><br/><span className="page-subtitle">Last run</span></p></div><div className="top-actions"><button className="btn btn-secondary"><Play size={16}/>Run</button><button className="btn btn-secondary"><Pause size={16}/>Pause</button><button className="btn btn-primary"><Activity size={16}/>View Logs</button></div></div>)}</div></section>}
